import RestockClientPage from "./RestockClientPage";

export const metadata = {
  title: "Restock Suggestions - StoreKeep",
  description: "AI-powered restock suggestions for your inventory.",
};

export default function RestockPage() {
  return <RestockClientPage />;
}
